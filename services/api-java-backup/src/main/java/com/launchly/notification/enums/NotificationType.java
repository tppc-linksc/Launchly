package com.launchly.notification.enums;

public enum NotificationType {
    ISSUE_ASSIGNED,
    ISSUE_FIXED,
    ISSUE_CLOSED,
    ISSUE_REOPENED,
    DEPLOY_SUCCEEDED,
    DEPLOY_FAILED,
    RELEASE_PUBLISHED,
    ROLLBACK_EXECUTED
}
