# Launchly 测试补全任务书

> **版本**：1.1
> **代码基线**：`6220fb6`（2026-08-13）
> **用途**：把测试补全拆成可独立执行、可严格复核的工作包。已知缺陷见[已知问题清单](./已知问题清单.md)，阶段退出见[交付验收规范](./交付验收规范.md)。
> **重要边界**：本任务书只授权测试和测试基础设施工作，不授权修复业务代码、修改产品行为或更新项目进度。

## 1. “测试要全”的定义

“全”不等于追求一个漂亮的覆盖率数字，而是同时满足：

1. **全量统计**：覆盖率工具必须统计全部生产源码，未被任何测试加载的文件也显示为 0%。
2. **场景完整**：正常、拒绝、边界、并发、重试、清理、跨 Workspace 和秘密不泄露均有测试。
3. **层级完整**：单元测试、模块/数据库集成测试、生产启动烟雾测试和真实 E2E 不能互相替代。
4. **断言有效**：断言最终状态、输出、错误或副作用；不能只断言 Mock 被调用。
5. **失败可解释**：若测试暴露业务缺陷，记录到[已知问题清单](./已知问题清单.md)，不得在测试任务中顺手改生产代码。

覆盖率只是最低门槛，显式场景矩阵必须 100% 完成。

## 2. 当前测试与覆盖率基线

### 2.1 代码和测试数量

| 范围 | 生产源码文件 | 测试文件 | 当前结果 |
| --- | ---: | ---: | --- |
| API | 87 个 `.ts`；覆盖率口径排除 `main.ts` 后为 86 个 | 7 个 spec | 7 suites / 52 tests 通过 |
| Web | 31 个 `.ts/.vue`，其中 24 个页面 | 3 个 spec | 3 files / 22 tests 通过 |
| CLI | 2 个 `.ts` | 1 个 spec | 1 file / 15 tests 通过 |

测试数量只能证明这些用例通过，不能代表 87 个 API 文件、24 个页面或 CLI 命令均被覆盖。

### 2.2 API 实测覆盖率

执行命令：

```bash
pnpm --dir services/api test:coverage
```

结果：

| 指标 | 覆盖 | 总量 | 覆盖率 |
| --- | ---: | ---: | ---: |
| Statements | 271 | 2307 | 11.74% |
| Branches | 129 | 996 | 12.95% |
| Functions | 37 | 376 | 9.84% |
| Lines | 227 | 1897 | 11.96% |

当前 Jest 配置统计全部 `src/**/*.ts`，仅排除 spec、测试辅助文件和明确留给生产启动烟雾测试的 `main.ts`。Module、DTO、Controller、Service、Worker、Runner、Guard、Filter 和 Config 均进入分母；JSON summary 实测包含 86/86 个符合条件的生产文件。

关键 0% 区域包括：

- `worker.service.ts` 和全部 Worker Runner。
- Project、Environment、Target、Test、Notification 的主要 Service。
- 16 个 Controller。
- 全局异常过滤、Edition 配置和大部分授权辅助逻辑。

### 2.3 Web 与 CLI 覆盖率

执行命令：

```bash
pnpm --dir apps/web test:coverage
pnpm --dir cli test:coverage
```

| 范围 | Statements | Branches | Functions | Lines | 文件完整性 |
| --- | ---: | ---: | ---: | ---: | ---: |
| Web | 5.65%（196/3465） | 39.13%（18/46） | 5.61%（5/89） | 5.65%（196/3465） | 31/31 |
| CLI | 10.13%（31/306） | 87.50%（7/8） | 83.33%（5/6） | 10.13%（31/306） | 2/2 |

Web 和 CLI 已建立全量测量能力，但低覆盖率和 CLI 分母较小意味着这些数字只能作为后续测试补全的真实起点，不能据此宣称核心场景受到充分保护。

## 3. 目标门槛

### 3.1 R0 全局最低门槛

| 范围 | Statements | Lines | Functions | Branches |
| --- | ---: | ---: | ---: | ---: |
| API | ≥ 80% | ≥ 80% | ≥ 80% | ≥ 70% |
| Web | ≥ 75% | ≥ 75% | ≥ 70% | ≥ 65% |
| CLI | ≥ 85% | ≥ 85% | ≥ 85% | ≥ 75% |

### 3.2 关键模块门槛

以下模块 Statements/Lines ≥ 90%、Functions ≥ 85%、Branches ≥ 80%：

- Auth、JWT/RBAC、ProjectAccess、SecretValue。
- Deployment、Worker 状态机和 RunnerFactory。
- BuildKit、OCI、Git、SSH Runner 的安全校验与清理分支。
- Environment Variable、Deploy Target、Release Gate。
- Web 登录/刷新、路由守卫、权限显示和 API 合同。

### 3.3 覆盖率不能替代的阻断测试

- Nest AppModule 生产模式启动烟雾测试。
- 两个 Workspace 的资源隔离集成测试。
- 空库/升级库 migration。
- API 与 Web 客户端合同测试。
- Docker 镜像实际构建和启动。
- Worker 租约并发、崩溃重领和无重复副作用。
- GitHub、Registry、BuildKit、SSH/BYOS、HTTPS、回滚真实 E2E。

## 4. 执行纪律

每次只委派一个 `TEST-*` 工作包。执行者必须遵守：

### 4.1 允许

- 只修改工作包白名单内的测试、测试配置和明确列出的依赖文件。
- 使用 Jest/Vitest、Nest TestingModule、临时目录、fake timers 和严格 Mock。
- 测试暴露缺陷时提供最小复现，不修改业务代码。

### 4.2 禁止

- 修改 `services/api/src`、`apps/web/src`、`cli/src` 中的生产文件，除非白名单明确列出。
- 删除、跳过、改名隐藏现有测试，使用 `.only`、`.skip`、`todo` 或无断言用例。
- 缩小 `collectCoverageFrom`/coverage include，增加无理由 exclude 或 Istanbul/V8 ignore。
- Mock 被测对象本身，或者仅断言 `toHaveBeenCalled()` 就声称行为正确。
- 访问真实网络、Docker、SSH、用户主目录或长期目录。
- 修改 README、路线图、已知问题状态、验收状态或声称整个项目完成。
- 自动提交、推送、部署或清理不属于测试创建的文件。

### 4.3 必交证据

```text
任务 ID：
代码基线：
修改文件（git diff --name-only）：
新增测试名称和对应场景：
执行命令：
完整原始结果（suite/test/coverage/exit code）：
失败或发现的新问题：
未覆盖范围：
是否修改生产代码：必须为“否”或说明已停止任务
```

同时附：

```bash
git status --short
git diff --check
git diff --stat
git diff -- <本任务白名单文件>
```

## 5. 可立即交给 MiniMax 的低风险工作包

### 当前工作包状态

| ID | 状态 | 是否可立即委派 | 前置条件 |
| --- | --- | --- | --- |
| TEST-000 | 已验收 | 否 | `6220fb6`，Codex 独立复核通过 |
| TEST-API-01 | 待委派 | 是 | TEST-000 经 Codex 复核 |
| TEST-API-02 | 待前置 | 否 | TEST-API-01 经 Codex 复核 |
| TEST-API-03 | 待前置 | 否 | TEST-API-02 经 Codex 复核 |
| TEST-API-04 | 待前置 | 否 | TEST-API-03 经 Codex 复核 |
| TEST-API-05 | 待前置 | 否 | 前五批稳定 |
| TEST-API-06 | 待前置 | 否 | Worker 设计复核 |
| TEST-API-07 | 待前置 | 否 | Runner 安全边界复核 |
| TEST-API-08 | 被问题阻塞 | 否 | KI-003/004/005 修复方案确定 |
| TEST-WEB-01 | 被问题阻塞 | 否 | KI-008/009 修复 |
| TEST-WEB-02 | 待前置 | 否 | TEST-WEB-01 |
| TEST-CLI-01 | 待前置 | 否 | TEST-000 和 CLI 测试设计复核 |

工作包只有在 Codex 独立复跑和审查后才能从“待委派”改为“已验收”；MiniMax 自述不能更新本表。

### TEST-000：统一覆盖率基础设施

**目标**：API/Web/CLI 都能输出 text 和 JSON summary，并统计未被 import 的生产文件。

**允许修改**：

- `package.json`
- `services/api/package.json`
- `services/api/jest.config.ts`
- `apps/web/package.json`
- `apps/web/vite.config.ts`
- `cli/package.json`
- `cli/vitest.config.ts`（可新增）
- `pnpm-lock.yaml`

**必须完成**：

1. 安装与 Vitest `2.1.9` 匹配的 `@vitest/coverage-v8`，不升级其他框架。
2. 为三端增加稳定的 `test:coverage` 命令。
3. Web include `src/**/*.{ts,vue}`，CLI include `src/**/*.ts`，排除测试文件、类型声明和生成物；不能只统计被 import 的文件。
4. API 统一统计 `src/**/*.ts`；只允许排除 spec、生成物和明确由生产启动烟雾测试覆盖的 `main.ts`。Module/DTO 不得为提高数字而默认排除。
5. 暂不伪造高阈值；先生成统一口径基线，再由复核人写入 ratchet 门槛。

**验收命令**：

```bash
pnpm --dir services/api test:coverage
pnpm --dir apps/web test:coverage
pnpm --dir cli test:coverage
```

三条命令必须退出 0，并各自输出 Statements/Branches/Functions/Lines；JSON summary 的 total 必须存在。

**验收记录（2026-08-13）**：`6220fb6` 已由 Codex 独立执行三端覆盖率、API build、统一入口、JSON 文件数比对和 `git diff --check`。API/Web/CLI 分别统计 86/86、31/31、2/2 个符合条件的生产文件；TEST-000 仅代表测量基础设施完成，不代表 BASE-11 或测试保护网完成。

### TEST-API-01：秘密、Edition 和异常过滤单元测试

**允许新增/修改**：

- `services/api/src/environment/secret-value.service.spec.ts`
- `services/api/src/common/config/edition.config.spec.ts`
- `services/api/src/common/filters/global-exception.filter.spec.ts`

**SecretValueService 必测**：

1. v2 加密后不包含明文，解密恢复原文。
2. 同一明文两次密文不同（随机 IV）。
3. 篡改 tag/ciphertext 后解密失败。
4. 未知 key ID、非法版本、过短 payload 分别失败。
5. previous key 可解旧密文，`reencrypt()` 改用当前 key 且仍可解。
6. production 缺少 encryption key 时构造失败。
7. `mask()` 覆盖空值、1–4 字符、5 字符、长值。
8. 每个测试恢复 `process.env`，不得依赖执行顺序。

**EditionConfig 必测**：默认 selfhost、显式 selfhost、cloud 三种分支。

**GlobalExceptionFilter 必测**：

- 字符串/对象/数组形式的 HttpException。
- SecurityException → 403。
- 普通 Error 默认 500。
- 含“无权”/“不存在”的兜底映射。
- 响应结构含 statusCode、message 数组、合法 timestamp；不得泄露 stack。

### TEST-API-02：Environment 与变量服务单元测试

**允许新增/修改**：

- `services/api/src/environment/environment.service.spec.ts`
- `services/api/src/environment/environment-variable.service.spec.ts`

**EnvironmentService 必测**：

- 环境不存在。
- 环境所属 Project 不在当前 Workspace。
- 合法更新只写允许字段。
- 空字符串/undefined/null 的语义符合 DTO。
- domain、端口、部署目标等每个分支均有断言。

**EnvironmentVariableService 必测**：

- list 不返回 `encryptedValue`，只返回 maskedValue。
- create 先验证 Workspace 归属，再加密和保存。
- 非本 Workspace、环境不存在、变量不存在均拒绝。
- delete 必须按变量真实 Environment/Project 校验，不能只相信 URL 中的 environmentId。
- 敏感与非敏感值、description 空值、重复 key 的数据库错误传播。

若当前 list 缺少 Workspace 入参导致目标测试无法成立，只提交复现和失败证据，不修改 Service。

### TEST-API-03：项目授权、仓库检测和资源目录单元测试

**允许新增/修改**：

- `services/api/src/project/project-access.service.spec.ts`
- `services/api/src/project/repository-hints.service.spec.ts`
- `services/api/src/project/resource-catalog.service.spec.ts`

**ProjectAccessService 必测**：

- Project 不属于 Workspace 时拒绝。
- Workspace OWNER 允许访问。
- Project VIEWER/TESTER/DEVELOPER/ADMIN 的五级矩阵。
- 无 Project membership、未知角色、最低角色未知值。
- 不得只断言 Prisma 调用；必须断言返回或异常。

**RepositoryHintsService 必测**：

- GitHub HTTPS/SSH、GitLab、Gitee URL；非法 URL 返回 null。
- npm/pnpm/yarn 的 lockfile 和 packageManager 推断。
- build/start/test 缺失、端口参数、`PORT=`、branch 含 `/`。
- README 安装命令覆盖条件。
- 非 2xx、超时、非法 JSON、超过 2 MB、HEAD 405/404 fallback。
- 全部 fetch 必须 Mock，测试不能访问互联网。

**ResourceCatalogService 必测**：目录 ID 唯一、可部署/不可部署标识、要求说明和返回值不可被调用方污染。

### TEST-API-04：Release、Notification 和 Test Service 单元测试

**允许新增/修改**：

- `services/api/src/release/release.service.spec.ts`
- `services/api/src/notification/notification.service.spec.ts`
- `services/api/src/testcase/test.service.spec.ts`

**ReleaseService 必测**：创建/list/get、Release 不存在、全部 Gate 通过、未豁免失败阻断、全部失败被豁免、发布字段、豁免记录。

**NotificationService 必测**：只读取当前 user、时间倒序、全部已读只更新本人未读、create 的可选字段。

**TestService 必测**：

- TestCase CRUD 和不存在。
- 创建 TestRun 时只复制 ACTIVE 用例；0 个用例也可创建。
- TestRunCase 必须同时匹配 run ID 和 case ID。
- PASSED/FAILED/SKIPPED/PENDING 计数与 RUNNING/COMPLETED 状态。
- 空用例集合的完成语义必须显式测试并记录当前行为。

## 6. 需要较强复核的测试工作包

这些任务仍可委派，但必须在前四个低风险包稳定后逐个执行。

### TEST-API-05：DeployTarget Service

- 新增 `deploy-target.service.spec.ts`。
- 覆盖非 root、KEY-only、固定 Host Key、workRoot 正规化和根目录拒绝。
- 覆盖 create/update/delete/list 输出不泄露 encryptedCredential/hostKey。
- SSH2 Client 必须完整 Mock：ready/error/exec/stdout/stderr/close、Host Key 匹配、不完整诊断输出、Docker 失败、80 端口状态。
- 绝对禁止连接真实 SSH。

### TEST-API-06：Worker 状态机

- 新增 `worker.service.spec.ts`。
- 覆盖无任务、PENDING 领取、过期租约重领、事务内 select+update、最大重试、成功、失败、超时、非法 payload、未知 task type。
- 对每个阶段断言 Deployment/Task/StageLog 最终状态和 lease 清理。
- fake timers 必须恢复；不得实际启动 Schedule。

### TEST-API-07：Runner 与命令执行边界

- 为 `runner.factory`、`command.executor`、`git`、`buildkit`、`oci-image`、`remote-ssh`、`shell`、`template-source`、cleanup 分别建立 spec。
- 所有进程、文件系统、Prisma、SSH 和网络依赖均 Mock 或使用任务专属临时目录。
- 必测输入白名单、路径穿越、Host/User/ID/domain/port、digest、命令超时、日志脱敏、临时密钥/known_hosts/env/compose 清理。
- 不得通过把私有校验复制到测试里来“验证”同一份逻辑。

### TEST-API-08：Controller、DTO 与授权合同集成测试

- 使用 Nest TestingModule 和真实 ValidationPipe；不能直接调用 Controller 方法绕过 Guard/Pipe。
- 公开接口矩阵、401、403、五级角色和全部项目子资源双 Workspace 矩阵。
- 父 projectId 与子资源真实 projectId 不一致必须拒绝。
- 未声明字段、非法 enum/port/domain/ID、空 body 必须拒绝。
- 该任务会直接验证 KI-004/KI-005，业务修复前允许先提供失败复现，但不能提交长期红测到主分支。

### TEST-WEB-01：API client、认证 Store 与路由

- TEST-000 和 KI-008/KI-009 修复后执行。
- 使用 Axios Mock Adapter 或等价 Mock；不得真实请求 API。
- 覆盖 Authorization 注入、并发 401 只刷新一次、刷新失败清理、refresh 自身 401 不递归。
- 覆盖未登录/已登录/init 状态路由、刷新恢复和角色导航。
- 为每个 Web API 方法建立 method/path/body/params 合同表，重点验证 Notification、Gate、Issue transition。

### TEST-WEB-02：核心页面行为

- 优先覆盖 Login、Init、AppLayout、ProjectCreate/Detail、Environment、Deployment Detail、DeployTarget、Release Detail、Notification。
- 每页必须覆盖 Loading、Empty、Success、Failure、Forbidden 和关键按钮权限。
- 不使用大面积 snapshot 代替行为断言。

### TEST-CLI-01：CLI 命令

- 使用 `mkdtemp` 创建任务专属目录，测试结束清理。
- Mock `execSync`/Docker；不得执行真实 Docker/Compose。
- 覆盖 install dry-run、首次/重复 install、权限 0600、up/down/restart/status/logs/doctor/backup/restore/upgrade/uninstall 的命令参数和失败提示。
- 增加 shell 参数注入用例；如果当前命令拼接不安全，停止并记录新问题。

## 7. 不应直接交给低可信执行者的验收

以下需要 Codex/人工主导，因为 Mock 很容易制造虚假成功：

| 工作 | 对应验收 |
| --- | --- |
| AppModule 生产启动和 Controller 枚举 | BASE-02/03 |
| 双 Workspace + 真实 PostgreSQL 授权集成 | BASE-06/07/08 |
| 空库与上一基线升级 migration | BASE-04/05 |
| Docker 镜像构建、API/Worker 健康、SPA | BASE-02 |
| Worker 并发/崩溃重领 | E2E-19 |
| GitHub/Registry/BuildKit/SSH/HTTPS/回滚 | E2E-05、10、16、18 等 |

## 8. 建议委派顺序

一次只发一个任务，按以下顺序：

1. TEST-000：先让覆盖率可测。
2. TEST-API-01：纯函数和安全基础。
3. TEST-API-02：Environment/Variable。
4. TEST-API-03：ProjectAccess/RepositoryHints/Catalog。
5. TEST-API-04：Release/Notification/Test。
6. 每次由 Codex 审核 diff、测试有效性和覆盖率变化后，再决定是否发 TEST-API-05 以后任务。

不应把 TEST-000 至 TEST-API-07 一次性全部交给同一个执行者，否则无法确认覆盖率增长来自哪一批测试，也难以及时发现越界修改。

## 9. 可直接复制的执行提示词

```text
你现在只执行 Launchly《测试补全任务书》中的【任务 ID】。

开始前必须：
1. 阅读任务条目、允许修改白名单、禁止项和必交证据。
2. 输出 git status --short，确认并列出现有用户改动；不得覆盖或改写这些改动。
3. 阅读所有被测生产文件和现有测试辅助代码后，先列出测试用例名称，再写测试。

硬约束：
- 只能修改该任务白名单文件；禁止修改生产代码、README、路线图、已知问题和验收状态。
- 禁止删除/跳过测试、使用 only/skip/todo、缩小覆盖率统计、增加 coverage ignore、访问真实网络/Docker/SSH。
- 测试发现生产缺陷时停止在最小复现，报告实际结果；不得顺手修复。
- 不得提交、推送或声称项目/模块已经完成。

完成后必须原样提供：
- git status --short
- git diff --check
- git diff --stat
- git diff -- 白名单文件
- 本任务规定的测试命令和完整原始输出
- 修改前后 Statements/Branches/Functions/Lines
- 新增测试名称、各自验证行为、仍未覆盖范围

任何命令未执行、失败或输出被截断，都必须明确写“未验证”，不得推测通过。
```

## 10. Codex 复核清单

每批 MiniMax 结果由 Codex 独立检查：

1. diff 是否只在白名单内，是否触碰/掩盖用户已有改动。
2. 是否完整阅读了被测文件，测试名和分支是否与实现一致。
3. 是否 Mock 被测对象、只测调用次数、写 tautology 或复制实现逻辑。
4. 是否有 `.skip/.only/todo`、弱断言、大 snapshot、coverage ignore 或统计范围缩小。
5. 是否恢复 env/timer/Mock，清理临时文件，测试可重复运行。
6. 独立运行目标测试、全量测试、coverage、build 和 `git diff --check`。
7. 比较 JSON summary 的分母；分母异常下降视为覆盖率造假或配置错误。
8. 发现生产缺陷时新增/更新 `KI-###`，不得把红测当成测试任务完成。
9. 只有全部证据通过后，才更新 TEST 状态；不自动关闭 R0 或 BASE。

## 修订记录

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| 1.1 | 2026-08-13 | TEST-000 经独立复核验收；记录三端全量覆盖率基线和文件分母，开放 TEST-API-01 |
| 1.0 | 2026-08-13 | 建立真实覆盖率基线、R0 门槛、完整场景矩阵、MiniMax 低风险工作包和 Codex 复核规则 |
